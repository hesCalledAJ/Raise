<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".core.Raise"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wakee2">
        <activity
            android:name=".feature_exceptions.CrashActivity"
            android:exported="false"
            android:label="@string/title_activity_crash"
            android:theme="@style/Theme.Wakee2" />
        <activity
            android:name=".feature_alarm.presentation.ring.RingActivity"
            android:exported="false"
            android:label="@string/title_activity_ring"
            android:showWhenLocked="true"
            android:theme="@style/Theme.Wakee2"
            android:turnScreenOn="true" />

        <receiver
            android:name=".feature_alarm.data.AlarmReceiver"
            android:exported="false" />

        <service
            android:name=".feature_alarm.data.service.AlarmService"
            android:foregroundServiceType="specialUse" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Wakee2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>